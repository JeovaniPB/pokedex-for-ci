name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Echo the message
        run: echo "Hello, Jeovani!"
      - name: Dime la fecha actual
        run: date
      - name: ls -l
        run: ls -l
